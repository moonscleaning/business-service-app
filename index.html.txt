<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Request</title>
    <style>
        /* Mobile-friendly clean design */
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; margin: 0; background-color: #f4f6f8; color: #333; }
        .container { max-width: 480px; margin: 40px auto; background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); }
        h2 { text-align: center; color: #2c3e50; margin-bottom: 20px; }
        p.desc { text-align: center; color: #666; font-size: 0.95rem; margin-bottom: 30px; }
        
        label { font-weight: 600; font-size: 0.9rem; display: block; margin-top: 15px; margin-bottom: 5px; color: #34495e; }
        input, textarea { width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 8px; font-size: 1rem; box-sizing: border-box; transition: 0.3s; }
        input:focus, textarea:focus { border-color: #3498db; outline: none; }
        
        button { width: 100%; padding: 16px; background-color: #2980b9; color: white; border: none; border-radius: 8px; font-size: 1.1rem; font-weight: bold; cursor: pointer; margin-top: 30px; transition: 0.3s; }
        button:hover { background-color: #1c5980; }
        
        .footer { text-align: center; margin-top: 20px; font-size: 0.8rem; color: #aaa; }
    </style>
</head>
<body>

    <div class="container">
        <h2>Request a Service</h2>
        <p class="desc">Please fill out the form below. We will get back to you shortly.</p>
        
        <label for="name">Name</label>
        <input type="text" id="name" placeholder="Your Full Name">
        
        <label for="phone">Phone Number</label>
        <input type="tel" id="phone" placeholder="e.g. 604-123-4567">
        
        <label for="email">Email Address</label>
        <input type="email" id="email" placeholder="name@example.com">
        
        <label for="message">Details / Request</label>
        <textarea id="message" rows="5" placeholder="Please describe your request..."></textarea>
        
        <button onclick="sendOrder()" id="submit-btn">Submit Request</button>
        
        <div class="footer">
            &copy; 2025 My Local Business
        </div>
    </div>

    <script>
        function sendOrder() {
            const btn = document.getElementById('submit-btn');
            
            // Get values
            const data = {
                name: document.getElementById('name').value,
                phone: document.getElementById('phone').value,
                email: document.getElementById('email').value,
                message: document.getElementById('message').value
            };

            // Basic Validation
            if(!data.name || !data.email || !data.phone) { 
                alert("Please fill in all required fields (Name, Phone, Email)."); 
                return; 
            }

            // Show loading state
            btn.innerText = "Sending...";
            btn.disabled = true;

            // -------------------------------------------------------
            // ★ PASTE YOUR WEB APP URL BELOW (Keep the quotes '')
            // -------------------------------------------------------
            const scriptURL = 'https://script.google.com/macros/s/AKfycbxWjiXubJN3DGDg0s9uArganDjSIVWJoMJNi6nPrjT1q4awYRX536vgTnI8uGxotrIU5g/exec';

            fetch(scriptURL, {
                method: 'POST',
                mode: 'no-cors', // Important for Google Apps Script
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            })
            .then(() => {
                alert("Thank you! Your request has been sent successfully.");
                location.reload(); // Refresh the page
            })
            .catch(err => {
                console.error('Error!', err.message);
                alert("Error! Please try again later.");
                btn.innerText = "Submit Request";
                btn.disabled = false;
            });
        }
    </script>

</body>
</html>